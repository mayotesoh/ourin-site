---
import '../styles/global.css';
import SnsIcons from '../components/SnsIcons.astro';
import { withBase } from '../lib/url';
import { SITE_TITLE, SITE_TAGLINE, SITE_DESCRIPTION, SITE_URL, PROFILE_IMAGE, CONTACT_EMAIL } from '../consts';

interface Props {
  title?: string;
  description?: string;
  ogImage?: string;
}

const { title, description = SITE_DESCRIPTION, ogImage = PROFILE_IMAGE } = Astro.props;

const pageTitle = title ? `${title}｜${SITE_TITLE}` : `${SITE_TITLE}｜${SITE_TAGLINE}`;
const canonical = new URL(Astro.url.pathname, Astro.site ?? SITE_URL).toString();
const ogImageUrl = new URL(withBase(ogImage), Astro.site ?? SITE_URL).toString();
const year = new Date().getFullYear();

const home = withBase('/');
const contactHref = withBase('/contact');
const navItems = [
  { href: home, label: 'ホーム' },
  { href: withBase('/service'), label: '事業内容' },
  { href: withBase('/about'), label: 'プロフィール' },
  { href: withBase('/ourin'), label: '占い師 凰凛' },
  { href: withBase('/blog'), label: '活動報告' },
];
const path = Astro.url.pathname;
const isActive = (href: string) => (href === home ? path === home : path.startsWith(href));
---

<!doctype html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{pageTitle}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    <link rel="icon" href={withBase("/favicon.svg")} type="image/svg+xml" />
    <link rel="apple-touch-icon" href={withBase(PROFILE_IMAGE)} />
    <link rel="sitemap" href={withBase("/sitemap-index.xml")} />
    <link rel="alternate" type="application/rss+xml" title="松山倫子 活動報告" href={withBase("/rss.xml")} />

    <meta property="og:type" content="website" />
    <meta property="og:site_name" content={SITE_TITLE} />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content={ogImageUrl} />
    <meta name="twitter:card" content="summary_large_image" />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Shippori+Mincho:wght@500;600;700&family=Zen+Kaku+Gothic+New:wght@300;400;500;700&display=swap"
      rel="stylesheet"
    />
  </head>

  <body>
    <header class="site-header" id="site-header">
      <div class="header-inner container">
        <a class="brand" href={home}>
          <img class="brand-img" src={withBase(PROFILE_IMAGE)} alt="" width="44" height="44" />
          <span class="brand-text">
            <span class="brand-name">松山 倫子</span>
            <span class="brand-sub">Noriko Matsuyama ｜ 働く選択肢を、もっと自由に。</span>
          </span>
        </a>

        <button
          class="hamburger"
          id="hamburger"
          aria-label="メニューを開く"
          aria-expanded="false"
          aria-controls="nav-menu"
        >
          <span></span><span></span><span></span>
        </button>

        <nav class="nav" id="nav-menu">
          <ul class="nav-list">
            {
              navItems.map((item) => (
                <li>
                  <a href={item.href} class:list={['nav-link', { active: isActive(item.href) }]}>
                    {item.label}
                  </a>
                </li>
              ))
            }
            <li><a href={contactHref} class="nav-link nav-cta">お問い合わせ</a></li>
          </ul>
          <div class="nav-sns">
            <SnsIcons size={18} gap={16} />
          </div>
        </nav>
      </div>
    </header>

    <main>
      <slot />
    </main>

    <footer class="site-footer">
      <div class="container footer-inner">
        <p class="footer-name">松山 倫子<span>まつやま のりこ</span></p>
        <p class="footer-tagline">占いコミュニティ ／ テレアポ ／ 営業代行</p>

        <p class="footer-alias">
          <img src={withBase("/images/ourin-logo-ink.png")} alt="" width="60" height="85" loading="lazy" />
          <span>占い師としての名前<a href={withBase('/ourin')}><strong>凰凛（おうりん）</strong></a></span>
        </p>

        <SnsIcons size={20} gap={14} variant="circle" class="footer-sns" />

        <ul class="footer-nav">
          {navItems.map((item) => <li><a href={item.href}>{item.label}</a></li>)}
          <li><a href={contactHref}>お問い合わせ</a></li>
        </ul>

        {CONTACT_EMAIL && <p class="footer-contact"><a href={`mailto:${CONTACT_EMAIL}`}>{CONTACT_EMAIL}</a></p>}

        <p class="copyright">© {year} Noriko Matsuyama. All rights reserved.</p>
      </div>
    </footer>

    <script is:inline>
      (function () {
        // ハンバーガーメニュー
        var btn = document.getElementById('hamburger');
        var nav = document.getElementById('nav-menu');
        if (btn && nav) {
          btn.addEventListener('click', function () {
            var open = nav.classList.toggle('open');
            btn.classList.toggle('active', open);
            btn.setAttribute('aria-expanded', String(open));
          });
          nav.addEventListener('click', function (e) {
            if (e.target && e.target.tagName === 'A') {
              nav.classList.remove('open');
              btn.classList.remove('active');
              btn.setAttribute('aria-expanded', 'false');
            }
          });
        }

        // スクロールでヘッダーを濃くする
        var header = document.getElementById('site-header');
        var onScroll = function () {
          if (header) header.classList.toggle('scrolled', window.scrollY > 20);
        };
        onScroll();
        window.addEventListener('scroll', onScroll, { passive: true });

        // スクロールで要素をふわっと表示
        var targets = document.querySelectorAll('.reveal');
        if ('IntersectionObserver' in window) {
          var io = new IntersectionObserver(
            function (entries) {
              entries.forEach(function (entry) {
                if (entry.isIntersecting) {
                  entry.target.classList.add('is-visible');
                  io.unobserve(entry.target);
                }
              });
            },
            { rootMargin: '0px 0px -8% 0px', threshold: 0.08 }
          );
          targets.forEach(function (t) {
            io.observe(t);
          });
        } else {
          targets.forEach(function (t) {
            t.classList.add('is-visible');
          });
        }
      })();
    </script>
  </body>
</html>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 100;
    backdrop-filter: blur(10px);
    background: var(--header-bg);
    border-bottom: 1px solid transparent;
    transition: background 0.3s ease, border-color 0.3s ease;
  }
  .site-header.scrolled {
    background: var(--header-bg-scrolled);
    border-bottom-color: var(--border);
  }
  .header-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
    padding: 12px 0;
  }

  .brand { display: flex; align-items: center; gap: 12px; text-decoration: none; color: var(--text); }
  .brand-img { width: 44px; height: 44px; border-radius: 50%; object-fit: cover; border: 1px solid var(--border); box-shadow: var(--shadow-sm); }
  .brand-text { display: flex; flex-direction: column; line-height: 1.3; }
  .brand-name { font-family: var(--font-serif); font-size: 1.3rem; letter-spacing: 0.18em; }
  .brand-sub { font-size: 0.62rem; letter-spacing: 0.16em; color: var(--muted); white-space: nowrap; }

  .nav { display: flex; align-items: center; gap: 22px; }
  .nav-list { display: flex; align-items: center; gap: 2px; list-style: none; margin: 0; padding: 0; }
  .nav-link {
    display: inline-block;
    white-space: nowrap;
    padding: 8px 11px;
    font-size: 0.86rem;
    color: var(--text);
    text-decoration: none;
    border-radius: 999px;
    transition: color 0.2s ease, background 0.2s ease;
  }
  .nav-link:hover { color: var(--accent-deep); background: var(--panel-hover); }
  .nav-link.active { color: var(--accent); }
  .nav-cta {
    white-space: nowrap;
    background: linear-gradient(120deg, #0fb37f, #35d3a0 55%, #0b8f66);
    color: #ffffff !important;
    font-weight: 700;
    padding: 8px 20px;
    margin-left: 6px;
  }
  .nav-cta:hover { filter: brightness(1.06); }
  /* ヘッダーのSNSアイコンはスマホのメニュー内でだけ表示（PCはナビを優先） */
  .nav-sns { display: none; padding-left: 18px; border-left: 1px solid var(--border); }

  .hamburger {
    display: none;
    width: 34px;
    height: 26px;
    background: none;
    border: 0;
    padding: 0;
    cursor: pointer;
    flex-direction: column;
    justify-content: space-between;
  }
  .hamburger span { display: block; height: 2px; width: 100%; background: var(--accent); border-radius: 2px; transition: transform 0.3s ease, opacity 0.3s ease; }
  .hamburger.active span:nth-child(1) { transform: translateY(12px) rotate(45deg); }
  .hamburger.active span:nth-child(2) { opacity: 0; }
  .hamburger.active span:nth-child(3) { transform: translateY(-12px) rotate(-45deg); }

  .site-footer {
    margin-top: 80px;
    padding: 56px 0 28px;
    border-top: 1px solid var(--border);
    background: var(--footer-bg);
  }
  .footer-inner { text-align: center; display: flex; flex-direction: column; align-items: center; gap: 18px; }
  .footer-name { margin: 0; font-family: var(--font-serif); font-size: 1.5rem; letter-spacing: 0.24em; text-align: center; }
  .footer-name span { display: block; font-size: 0.62rem; letter-spacing: 0.3em; color: var(--muted); margin-top: 8px; }
  .footer-alias { display: flex; align-items: center; gap: 14px; margin: 0; }
  .footer-alias img { height: 58px; width: auto; opacity: 0.85; }
  .footer-alias span { font-size: 0.7rem; letter-spacing: 0.14em; color: var(--muted); line-height: 1.9; }
  .footer-alias a { text-decoration: none; }
  .footer-alias a:hover strong { color: var(--accent-deep); }
  .footer-alias strong { display: block; font-family: var(--font-serif); font-size: 0.95rem; letter-spacing: 0.2em; color: var(--text); font-weight: 600; }
  .footer-tagline { margin: 0; font-size: 0.85rem; color: var(--muted); letter-spacing: 0.16em; }
  .footer-sns { justify-content: center; }
  .footer-nav { display: flex; flex-wrap: wrap; justify-content: center; gap: 8px 22px; list-style: none; margin: 0; padding: 0; }
  .footer-nav a { color: var(--muted); text-decoration: none; font-size: 0.85rem; transition: color 0.2s ease; }
  .footer-nav a:hover { color: var(--accent-deep); }
  .footer-contact a { color: var(--muted); font-size: 0.85rem; }
  .copyright { margin: 6px 0 0; font-size: 0.72rem; color: var(--muted-soft); letter-spacing: 0.08em; }

  /* 画面が狭くなったら、ヘッダーの要素を段階的に省く */
  @media (max-width: 1080px) {
    .brand-sub { display: none; }
    .nav-link { padding: 8px 9px; font-size: 0.83rem; }
  }

  @media (max-width: 900px) {
    .hamburger { display: flex; }
    .brand-sub { display: block; }
    .nav {
      position: absolute;
      top: 100%;
      left: 0;
      width: 100%;
      flex-direction: column;
      align-items: stretch;
      gap: 0;
      padding: 0 5%;
      background: rgba(255, 255, 255, 0.98);
      border-bottom: 1px solid var(--border);
      max-height: 0;
      overflow: hidden;
      transition: max-height 0.35s ease, padding 0.35s ease;
    }
    .nav.open { max-height: 80vh; padding: 12px 5% 24px; overflow-y: auto; }
    .nav-list { flex-direction: column; align-items: stretch; gap: 6px; }
    .nav-link { text-align: center; padding: 14px; border: 1px solid var(--border); }
    .nav-cta { margin-left: 0; }
    .nav-sns { display: flex; padding: 18px 0 0; border-left: 0; justify-content: center; }
    .header-inner { position: relative; }
  }
</style>
